# Tier 0 live evidence — 2026-07-22

This record is structural and contains no audio, transcript, prompt, response,
speaker identity, household target, or tool arguments.

## Production streaming probe

- Backend: `vllm-qwen3-tts` production service
- Streaming chunks: 116
- First PCM: 1,308.761 ms
- PCM bytes: 472,320
- Largest raw arrival gap: 1,442.561 ms
- Worst playback pacing deficit after accumulated PCM: 0 ms
- GPU free/total before recovery: 1,445 / 11,264 MiB
- Result: pass for multi-chunk production streaming and buffered pacing

## Residency fault found by the gate

The first aggregate health probe failed because `llama-server` was resident but
unresponsive. Systemd showed the process in uninterruptible `D` state at its
5 GiB `MemoryHigh`, with 3.5 GiB swapped. Its upstream prompt cache default was
8 GiB. The service was restarted to restore availability, and the checked-in
unit now caps prompt cache RAM at 1,024 MiB. Restarting that required model also
restarted dependent TTS and Voice services, invalidating the previous residency
age for Tier 0 acceptance.

After recovery, all four Voice/model services were active and the authenticated
Nova Voice aggregate health endpoint returned `ok: true`, including dashboard,
LLM, STT, TTS, denoising, and speaker-recognition subchecks.

## Physical/browser-path trial

The existing physical E2E harness sent one harmless year query and recorded the
available Brio browser/USB microphone for 12 seconds. It heard no satellite
reply (`peak_db: -77.9`), so this trial is retained as a failed setup attempt and
is not counted as acceptance-corpus evidence.

## Remaining gate

The 24-hour monitor must start after the prompt-cache fix is deployed and all
required services are stable. M0-06 remains incomplete until it produces a
passing 86,400-second report and the eleven far-field, echo/barge-in,
false-activation, and spoken-number corpus cases have recorded passing evidence.
